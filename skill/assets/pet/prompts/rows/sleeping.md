Create one coherent horizontal interaction animation strip for pet `daisy`.

Use the attached canonical idle frame as the exact identity reference and the layout guide only for invisible spacing. Preserve the same face, body proportions, silhouette, palette, materials, markings, clothing, and props.

State: `sleeping`.
Purpose: settle into a quiet breathing sleep.
Output exactly 6 separated, centered, complete full-body poses from left to right. The final pose must connect naturally back to the first pose. All six poses must show the pet already asleep in the same seated or curled resting posture, with closed eyes and only small breathing changes. Do not use a standing pose. Keep the umbrella folded and touching the pet. No Z letters, sleep symbols, or detached marks.

Use one flat pure blue #0000FF background. Distribute the poses evenly across the full canvas width. Scale the complete pet and every attached prop to fit inside its own invisible equal-width slot. Leave at least 32 pixels of uninterrupted pure background from top to bottom between every two adjacent poses. No silhouette, hair, clothing, prop, effect, or antialiased edge may touch or overlap another pose. Keep a stable baseline. Do not include visible grid lines, labels, interface elements, shadows, scenery, glow, blur, motion streaks, or detached particles. Do not use #0000FF inside the pet.
