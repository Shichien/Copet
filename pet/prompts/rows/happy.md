Create one coherent horizontal interaction animation strip for pet `daisy`.

Use the attached canonical idle frame as the exact identity reference and the layout guide only for invisible spacing. Preserve the same face, body proportions, silhouette, palette, materials, markings, clothing, and props.

State: `happy`.
Purpose: show a calm positive reaction distinct from celebration.
Output exactly 6 separated, centered, complete full-body poses from left to right. The final pose must connect naturally back to the first pose. Keep the reaction calmer and smaller than the celebrate animation.

Use one flat pure blue #0000FF background. Keep every pose inside its own invisible slot with generous padding and a stable baseline. Do not include visible grid lines, labels, interface elements, shadows, scenery, glow, blur, motion streaks, or detached particles. Do not use #0000FF inside the pet.
